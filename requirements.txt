# === FastAPI Web App Dependencies ===
fastapi>=0.116.1
uvicorn>=0.35.0
jinja2>=3.1.5

# === Static File Handling ===
python-multipart>=0.0.6  # For file uploads

# === ML Core ===
tensorflow==2.18.0       # Ensure compatibility with TFViTModel
keras==3.9.0             # Newer Keras, should work with TF 2.18+
transformers==4.53.2     # For TFViTModel
torch==2.6.0             # If needed for other models (optional)

# === Image + Data Processing ===
pillow>=11.0.0
numpy>=2.0.0
matplotlib>=3.10.0
pandas>=2.2.0
